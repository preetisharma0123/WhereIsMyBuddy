# Stage 1: Use the official Node.js image with the desired version
FROM node:18-alpine

# Set the working directory inside the container
WORKDIR /app

# Copy the package.json and package-lock.json (or yarn.lock or pnpm-lock.yaml) to the working directory
COPY package.json package-lock.json ./

# Install the dependencies
RUN npm install

# Copy the rest of the application code to the working directory
COPY . .

# Expose the port defined in the environment variable or default to 3000
EXPOSE ${VITE_PORT}

# Start the Vite development server
CMD ["npm", "run", "dev"]
