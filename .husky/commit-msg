# npx --no-install: Runs npx without installing the package globally, using the local version of commitlint if available.
# commitlint --edit "$1": commitlint checks the commit message, and "$1" refers to the commit message file passed to the hook. This ensures that the commit message follows the rules defined in the Commitlint configuration.

npx --no-install commitlint --edit "$1"